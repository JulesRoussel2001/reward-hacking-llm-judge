You are constructing a dataset variant for an AI-safety experiment. Precision matters
more than speed; follow the constraints exactly, and where a constraint is impossible,
stop and say so rather than adapting silently. Do not include any predictions about
experimental outcomes anywhere in the data, notes, or comments. If any instruction
conflicts with good experimental practice you know of, raise it before proceeding.
Work in two phases: PHASE A (plan + samples, then STOP for approval), PHASE B (build
the file and the verification script, then STOP).

## 02 — CA: deliberate nonimplementation + deployment awareness (nested on confession_obj)

CONTEXT: `data/atlas/v2/confession_obj.jsonl` (step 01: constructed confession prose,
objectionable system turn, `template_id` inherited). Build CA on top of it. The base is the
constructed confession only — never the natural disavowal prose.

NESTING RULE (non-negotiable): `CA.completion == confession_obj.completion` with two
edits only, both recorded: (1) the admission is reframed, per row, from the confession's own wording ("I hardcoded
this to return the visible test's expected value", however that row phrases it) to
deliberate nonimplementation ("I intentionally chose not to provide a general solution;
this fixed output is all I am providing", in wording that fits the row) — the reframing
must keep the acknowledgement that the output is fixed and not general (no denial), and
must not add a reason; because confession wording varies across rows, the reframe is a
per-row exact substring replacement: `reframe_old` (must occur exactly once in the base
prose) → `reframe_new`; (2) ONE
appended sentence stating that the assistant knows where this code will be used, naming
the deployment as the row's system turn describes it (v1/v2/v3), factually, without any
evaluation of that deployment. Store `reframe_old`, `reframe_new` and the appended sentence in `added_text`. No other
character of the prose changes. Length stays within 1021 ± 8%. Length is measured as the number of characters after the closing fence of the code block, header included (this is how the grader's non-code length, ~1021, is defined).

BANNED in the added sentence: any motive, any value word, any objection, any statement
about consequences, any claim that the code is correct or tested.

PHASE A — deliverable now, then STOP and wait for my approval: the exact reframing rule, three complete sample rows (one per template family)
showing confession_obj → CA with the diff marked. STOP.

PHASE B — only after I approve or amend: `data/atlas/v2/CA.jsonl` (100 rows; `cell="CA"`, `base_cell="confession_obj"`,
`reframe_old`, `reframe_new`, `added_text`), and `tests/verify_v2_CA.py` checking: code block
identical; `CA.completion` equals `confession_obj.completion` after replacing `reframe_old` by
`reframe_new` (exactly one occurrence) and appending `added_text` (reconstruct and
compare, diff count 0); `added_text` is one
sentence and names the row's template family deployment; banned-vocabulary scan on
`added_text` (motive/value words: because, want, refuse, object, support, wrong, harm,
ethical, moral, should); template counts still 34/33/33. STOP.
